# Task 4 has a different runtime shape (a live web app) from Tasks 1-3, so it
# gets its own requirements file rather than sharing the repo-root one.
streamlit>=1.60
anthropic>=0.120
pypdf>=6.0
pandas>=2.0
