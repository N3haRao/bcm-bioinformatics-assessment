# Core scientific stack used across all three tasks.
# Tested with: numpy 1.26.4, scipy 1.10.1, matplotlib 3.7.1, scikit-learn 1.2.2,
# statsmodels 0.13.5 (Python 3.11).
#
# Task 1 (task1_gtf_features/) uses only the Python standard library beyond
# this list (gzip, argparse, heapq) -- no extra dependency needed there.
numpy>=1.24
scipy>=1.9
matplotlib>=3.6
scikit-learn>=1.1
statsmodels>=0.13
